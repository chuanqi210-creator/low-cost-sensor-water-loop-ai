# 操作记录

## 2026-06-23

- 从个人文档区旧工作目录复制正式项目副本到当前仓库。
- 复制时排除 `.venv/`、`.pytest_cache/`、`__pycache__/`、`*.pyc` 和 `.DS_Store`。
- 补充 `AGENTS.md`，明确 Codex 进入项目后的操作边界和验证方式。
- 补充 `来源记录.md`，记录正式项目路径、迁移背景和复制原则。
- 在 `.gitignore` 中加入 `.worktrees/` 与 `worktrees/`，避免后续工作树被误提交。
- 初始化独立 Git 仓库，默认分支为 `main`，并提交项目基线。
- 已验证可以从 `main` 创建临时工作树 `.worktrees/codex-smoke-20260623`，验证后已删除临时工作树和临时分支。
- 初始复制当天，系统 `python3` 为 3.9.6，不满足项目 `pyproject.toml` 的 Python 3.12 要求；独立化整理后改为在本仓库创建并使用 `.venv/`。
- 初始基线测试结果：`663 passed in 22.26s`。
- 按“活入口 + 关键产物刷新”完成独立化整理：`AGENTS.md`、`README.md`、`来源记录.md`、`docs/field_data_interface_spec.md` 等入口文件不再把旧目录作为依赖。
- 使用 Codex bundled Python 3.12.13 在本仓库创建 `.venv/`，安装 `requirements.txt` 并以 editable 方式安装当前项目包。
- 刷新 formal search handoff/operator packet、Agent44、R7 pipeline、Agent61、external activation router、stage board、Agent60 和 Agent50 关键产物，使活入口路径指向当前仓库。
- 新增独立化回归测试，保护活入口和关键治理产物不再路由到迁移前目录。
- 独立环境完整回归：`.venv/bin/python -m pytest -q`，`665 passed in 21.52s`。
- 已将 Codex 能力发现与阶段复盘流程写入 `AGENTS.md`，要求任务开始和关键阶段主动搜索/评估 skills、工具、插件和 API，并在最终汇报中说明使用情况。
- 启动本仓库 review-loop 第 0 轮，产出 `docs/reviews/2026-06-23-architecture-review.md`，明确核心行为契约、field evidence/no-write 边界、runner/source 热点、第一轮低风险优化入口和验收命令。
- 按 review-loop 第一轮执行低风险行为保持优化：为 `experiments/run_formal_search_nonlegal_review_operator_packet.py` 抽出 JSON/text 写入和项目相对路径 helper，不改变 packet schema、manifest key 或输出路径；验证 `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_formal_search_nonlegal_review_operator_packet.py` 为 `5 passed in 0.02s`。
- 根据只读复核意见补齐 formal-search handoff 旧路径 rebasing 覆盖范围，新增对通用 legacy fixture 根的测试覆盖，并刷新 preliminary formal search handoff 与 operator packet，使 `FORMAL_SEARCH_RESULT_PACKAGE_PATH` 回到当前仓库。
- 补强项目独立性回归测试，将通用 legacy fixture 根纳入旧根检查；机械替换活入口文件中的旧根为当前仓库路径，验证 `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_formal_search_nonlegal_review_operator_packet.py tests/test_project_independence.py` 为 `6 passed in 0.02s`。
- 面向开源发布重写 `README.md`：参考高星项目 README 的首屏表达和流程化结构，新增系统 workflow、每一步用途说明、受保护边界、贡献 review-loop 与 open-source hygiene，移除首页内部迭代流水账和本地化叙事。
- 新增 MIT `LICENSE`，并将 `README.md` 的 License 段落从未授权提示改为 MIT License 引用，补齐开源发布前的许可证门槛。
- 将 MIT `LICENSE` 版权持有人更新为 `chuanqi chen`。
- 面向开源发布将 legacy path fixture 和操作记录中的个人绝对路径脱敏为通用路径描述，避免公开仓库暴露本机用户名和历史目录。
- 安装并配置 GitHub CLI `gh` 2.95.0，完成 `chuanqi210-creator` 账号登录；将本仓库后续提交作者设置为 `Chuanqi Chen <288862260+chuanqi210-creator@users.noreply.github.com>`。
- 评估 `mattpocock/skills` 的 34 个外部技能，确认其已存在于 `~/.codex/skills` 但当前会话未注入；新增 `CONTEXT.md`、`docs/agents/` 配置和 `docs/skills/mattpocock-skills-evaluation.md`，把外部 skill 发现、试点、部署和重启要求纳入项目流程。
- 在 GitHub 仓库补齐 mattpocock/skills triage 需要的 labels：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`；沿用已有 `wontfix`。
